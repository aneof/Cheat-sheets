# print status of branch
git status

# add specific (or all) files to commit
git add .

# commit changes with a message (necessary)
git commit -m "message"

# push changes. origin is the default remote repository name, master is the branch you're on.
git push origin master

# switch to another branch
git checkout master

